'use client'
import Link from 'next/link';import { useEffect,useState } from 'react';import { createSupabaseBrowserClient } from '../../src/lib/supabase/browser';import styles from '../watchlist/watchlist.module.css'
type Session={configured:boolean;user:null|{email?:string;displayName:string|null;avatarUrl:string|null}}
export default function AccountClient(){const[s,setS]=useState<Session|null>(null);useEffect(()=>{fetch('/api/account/session').then(r=>r.json()).then(x=>setS(x.data))},[]);const signin=async()=>{const c=createSupabaseBrowserClient();if(c)await c.auth.signInWithOAuth({provider:'google',options:{redirectTo:`${location.origin}/auth/callback?next=/account`}})};const signout=async()=>{await fetch('/api/account/signout',{method:'POST'});location.href='/'};if(!s)return <main className={styles.page}><div className={styles.empty}>Loading account...</div></main>;return <main className={styles.page}><header className={styles.hero}><div><span>CredoNomics account</span><h1>Account</h1><p>A compact home for your saved market workspace.</p></div></header>{!s.configured?<div className={styles.empty}><h2>Cloud accounts are not configured yet.</h2><p>Your device watchlists remain available and unchanged.</p></div>:!s.user?<div className={styles.empty}><h2>Save your workspace across devices.</h2><p>Sign in with Google. CredoNomics requests only your basic profile identity.</p><button className={styles.button} onClick={signin}>Continue with Google</button></div>:<div className={styles.panel}><h2>{s.user.displayName||s.user.email}</h2><p className={styles.muted}>{s.user.email}</p><div className={styles.form}><Link className={styles.button} href='/watchlist'>Watchlists</Link><Link className={`${styles.button} ${styles.secondary}`} href='/alerts'>Alerts</Link><Link className={`${styles.button} ${styles.secondary}`} href='/account/notifications'>Notifications</Link><button className={`${styles.button} ${styles.danger}`} onClick={signout}>Sign out</button></div></div>}</main>}
