import { Database, FileCheck2, RefreshCcw, ShieldCheck } from 'lucide-react'
import SiteFrame from '../../components/SiteFrame'
import styles from '../../core-v4.module.css'
import v6 from '../../v6.module.css'

export const metadata={title:'Credit Card Data Standard',description:'The fields and verification rules CredoNomics will use for structured Indian credit-card research.'}
const groups=[['Identity','Issuer, product name, network and product status.'],['Ownership cost','Joining fee, annual fee, applicable tax and fee-waiver threshold.'],['Reward engine','Base rate, accelerated categories, caps, cap periods and redemption value.'],['Exclusions','Transactions or merchant categories that do not earn the expected benefit.'],['Travel & fuel','Lounge rules, forex markup and fuel surcharge-waiver conditions.'],['Verification','Official source links, effective dates and a last-verified date.']]
export default function Page(){return <SiteFrame><section className={`${styles.wrap} ${styles.pageHero}`}><div className={styles.breadcrumbs}><a href="/">Home</a><span>/</span><a href="/research">Research</a><span>/</span><span>Card data standard</span></div><span className={styles.pageKicker}><Database size={14}/> Research infrastructure</span><h1>A card comparison is only as good as its <span>data model.</span></h1><p className={styles.pageHeroLead}>This is the structured field standard being used to build CredoNomics toward a source-backed Indian credit-card comparison engine.</p></section><section className={`${styles.wrap} ${styles.pageBody} ${v6.shell}`}><div className={v6.dataGrid}>{groups.map(([h,p],i)=><article className={v6.dataCard} key={h}><small>0{i+1}</small><h3>{h}</h3><p>{p}</p></article>)}</div><div style={{height:14}}/><div className={v6.correctionBox}><ShieldCheck size={22}/><h2>No invented card database.</h2><p>CredoNomics should not publish a product record until current official source documents and a last-verified date are attached. The public comparison engine can grow as verified records are added.</p></div></section></SiteFrame>}
