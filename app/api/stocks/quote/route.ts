import {NextRequest,NextResponse} from "next/server";import {getMarketDataProvider} from "../../../../src/services/market-data/market-data-service";export const dynamic="force-dynamic";export async function GET(request:NextRequest){const key=request.nextUrl.searchParams.get("instrumentKey")?.trim();if(!key||!(/^(NSE|BSE)_EQ\|INE[A-Z0-9]{9}$/.test(key)))return NextResponse.json({data:null,error:{code:"INVALID_REQUEST",message:"A valid equity instrument key is required."}},{status:400});const result=await getMarketDataProvider().getQuote(key);return NextResponse.json(result,{status:result.error?.code==="AUTH_REQUIRED"?503:result.error?502:200,headers:{"Cache-Control":"private, max-age=10, stale-while-revalidate=20"}})}
