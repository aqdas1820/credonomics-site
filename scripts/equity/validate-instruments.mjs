import fs from 'node:fs';import path from 'node:path';import {validateInstruments,writeJson} from './instruments-lib.mjs';const root=path.resolve('data/equity-instruments/staging'),file=path.join(root,'instruments.json'),payload=JSON.parse(fs.readFileSync(file,'utf8')),result=validateInstruments(payload.instruments??[]),errors=[];if(payload.schemaVersion!=='equity-instruments-v1')errors.push('invalid schemaVersion');if(result.rejected.length)errors.push(`${result.rejected.length} malformed published instruments`);if(result.duplicates.length)errors.push(`${result.duplicates.length} duplicate published instruments`);if(result.valid.length!==payload.counts?.valid)errors.push('published count mismatch');writeJson(path.join(root,'.validation.json'),{status:errors.length?'FAILED':'PASSED',errors,validatedAt:new Date().toISOString()});if(errors.length){errors.forEach(console.error);process.exit(1)}console.log(`Equity instrument staging valid: ${result.valid.length} securities.`)
